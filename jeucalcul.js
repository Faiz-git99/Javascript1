// Déclarer une fonction qui va calculer l'âge automatiquement

function calculeAuto(param1,param2){
let age = param1 - param2;
   return(age);
}
let result = (2024-2000);

let classe= document.querySelector("auto");
.appendChild(result);